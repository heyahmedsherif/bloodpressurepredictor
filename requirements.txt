# Blood Pressure Predictor - Streamlit Cloud Deployment Requirements
# Optimized for Python 3.11 and dependency compatibility

# Core dependencies with proven compatibility
streamlit>=1.28.0
numpy>=1.21.0,<2.0.0
pandas>=1.3.0,<2.0.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
seaborn>=0.11.0
plotly>=5.15.0

# ML framework - using CPU-only version for Streamlit Cloud
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0

# Signal processing
scipy>=1.9.0
peakutils>=1.3.0

# Utilities
tqdm>=4.64.0
joblib>=1.1.0
dotmap>=1.3.0

# Optional dependencies for extended functionality
# Note: Some advanced PPG processing features may be limited without these
# pyPPG - omitted due to pandas version conflicts
# torch-ecg - omitted due to version availability on Python 3.13

# For development/testing only (commented out for deployment)
# openpyxl>=3.0.0
# vitaldb>=1.0.0
# pytorch_metric_learning>=2.0.0