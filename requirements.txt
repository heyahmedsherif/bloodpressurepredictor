# Multi-architecture Railway Requirements with WebRTC Camera Support
# Optimized for ARM64/x86_64 compatibility

# Core Streamlit with dependencies  
streamlit>=1.28.1
blinker>=1.5.0
altair>=4.0
protobuf>=3.20.0
pyarrow>=6.0
click>=7.0
tornado>=6.0.3
watchdog>=2.1.3

# Essential data processing with architecture compatibility
numpy>=1.24.4
pandas>=2.0.3

# Visualization
plotly>=5.17.0
matplotlib>=3.8.2

# Camera support - use headless version for container compatibility
opencv-python-headless>=4.8.1.78

# WebRTC for real-time camera access with explicit versions for stability
streamlit-webrtc==0.47.0
aiortc==1.6.0

# Explicit dependencies for aiortc/WebRTC stability (minimal for emergency build)
av>=10.0.0
aioice>=0.9.0
aiohttp>=3.8.0

# Basic ML (without heavy models)
scikit-learn>=1.3.2
scipy>=1.11.4
joblib>=1.3.2

# Utilities
pillow>=10.1.0
tqdm>=4.66.1
PyYAML>=6.0.1
yacs>=0.1.8
requests>=2.28.1

# Additional stability packages
cffi>=1.15.0
cryptography>=41.0.0
