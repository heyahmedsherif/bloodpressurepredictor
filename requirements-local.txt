# Local Development Requirements - Fixed Versions
# Pinned versions to avoid numpy/pandas compatibility issues

# Core compatible versions (tested together)
numpy==1.24.4
pandas==2.0.3
scikit-learn==1.3.2

# Core Streamlit and visualization
streamlit==1.28.1
matplotlib==3.8.2
plotly==5.17.0
scipy==1.11.4
joblib==1.3.2

# Essential ML dependencies
tqdm==4.66.1
PyYAML==6.0.1
yacs==0.1.8

# Camera support - full OpenCV with GUI support for local development
opencv-python==4.8.1.78

# PyTorch CPU (compatible versions)
torch==2.1.1
torchvision==0.16.1

# Real WebRTC camera support (works locally without TURN servers)
streamlit-webrtc==0.47.7
aiortc==1.6.0

# Additional compatible packages
pillow==10.1.0
twilio==8.10.3